import { NoWordDirective } from './no-word.directive';

describe('NoWordDirective', () => {
  it('should create an instance', () => {
    const directive = new NoWordDirective();
    expect(directive).toBeTruthy();
  });
});
